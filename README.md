# Kata Wardrobe Generator

## Installation

### `docker-compose build`
### `docker-compose up -d`

For install the dependencies, go inside the container
### `docker-exec -it container_name bash`

Inside, we run 
### `dep ensure`
